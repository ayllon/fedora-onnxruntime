diff --git a/cmake/CMakeLists.txt b/cmake/CMakeLists.txt
index e1b4c053c..a138074d0 100644
--- a/cmake/CMakeLists.txt
+++ b/cmake/CMakeLists.txt
@@ -704,9 +704,15 @@ if (onnxruntime_BUILD_BENCHMARKS)
   endif()
 endif()
 
-if (NOT WIN32 AND NOT onnxruntime_PREFER_SYSTEM_LIB)
-  add_subdirectory(${PROJECT_SOURCE_DIR}/external/nsync EXCLUDE_FROM_ALL)
+if (NOT WIN32)
+  if (onnxruntime_PREFER_SYSTEM_LIB)
+    find_library(nsync NAMES nsync)
+  endif()
+  if (NOT nsync_FOUND)
+    add_subdirectory(${PROJECT_SOURCE_DIR}/external/nsync EXCLUDE_FROM_ALL)
+  endif()
 endif()
+
 # External dependencies
 list(APPEND CMAKE_MODULE_PATH ${PROJECT_SOURCE_DIR}/external)
 
@@ -825,6 +831,14 @@ target_include_directories(safeint_interface INTERFACE ${SAFEINT_INCLUDE_DIR})
 
 if (onnxruntime_PREFER_SYSTEM_LIB)
   find_package(boost_mp11)
+  if (NOT TARGET Boost::mp11)
+    find_package(Boost 1.66)
+    if (Boost_FOUND)
+      add_library(boost_mp11 INTERFACE)
+      add_library(Boost::mp11 ALIAS boost_mp11)
+      target_include_directories(boost_mp11 INTERFACE ${Boost_INCLUDE_DIRS})
+    endif()
+  endif()
 endif()
 if (NOT TARGET Boost::mp11)
   add_subdirectory(external/mp11 EXCLUDE_FROM_ALL)
@@ -832,7 +846,15 @@ endif()
 
 set(JSON_BuildTests OFF CACHE INTERNAL "")
 set(JSON_Install OFF CACHE INTERNAL "")
-add_subdirectory(external/json EXCLUDE_FROM_ALL)
+if (onnxruntime_PREFER_SYSTEM_LIB)
+  find_package(nlohmann_json)
+  if (nlohmann_json_FOUND)
+    include_directories(${nlohmann_json_INCLUDE_DIRS})
+  endif()
+endif()
+if (NOT nlohmann_json_FOUND)
+  add_subdirectory(external/json EXCLUDE_FROM_ALL)
+endif()
 
 if (onnxruntime_PREFER_SYSTEM_LIB)
   find_package(re2)
@@ -1410,7 +1432,11 @@ if (onnxruntime_PREFER_SYSTEM_LIB)
   find_package(Flatbuffers)
 endif()
 if (Flatbuffers_FOUND)
-  add_library(flatbuffers ALIAS flatbuffers::flatbuffers)
+  if (TARGET flatbuffers::flatbuffers)
+    add_library(flatbuffers ALIAS flatbuffers::flatbuffers)
+  else()
+    add_library(flatbuffers ALIAS flatbuffers::flatbuffers_shared)
+  endif()
 else()
   add_subdirectory(external/flatbuffers EXCLUDE_FROM_ALL)
 endif()
